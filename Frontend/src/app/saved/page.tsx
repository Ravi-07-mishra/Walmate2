import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function SavedItemsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Saved Items</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder page for your saved items.</p>
      </CardContent>
    </Card>
  );
}
